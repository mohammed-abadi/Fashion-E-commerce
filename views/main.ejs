<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Fashion-E-commerce</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>

<header class="header">
  <div class="container header-container">
    <h1 class="logo">
      Fashion-E-commerce
    </h1>

    <nav class="nav">
      <a href="#home" class="nav-link">Home</a>
      <a href="#categories" class="nav-link">Categories</a>
      <a href="#offers" class="nav-link">Offers</a>
      <a href="#products" class="nav-link">Products</a>
      <a href="#contact" class="nav-link">Contact</a>
      <div class="cart-nav">
        <a href="/cart" class="nav-link">Cart</a>
        <span class="cart-icon-badge">🛒 <span id="cart-count">0</span></span>
      </div>
    </nav>

    <div class="auth-buttons">
      <% if (locals.user) { %>
        <a href="/profile" class="btn profile-btn">👤 Profile</a>
        <span class="username">Welcome, <%= user.first %> <%= user.last %></span>
        <a href="/auth/sign-out" class="btn logout-btn">Logout</a>
      <% } else { %>
        <a href="/auth/sign-in" class="btn signin-btn">Sign In</a>
        <a href="/auth/sign-up" class="btn signup-btn">Sign Up</a>
      <% } %>
    </div>
  </div>
</header>

<section id="home" class="hero">
  <div class="container hero-content">
    <h2 class="hero-title">New Collection 2026</h2>
    <p class="hero-subtitle">Upgrade your style with the latest trends</p>
    <a href="#products" class="btn hero-btn">Shop Now</a>
  </div>
</section>

<section id="categories" class="categories">
  <div class="container">
    <h2 class="section-title">Shop by Category</h2>
    <div class="category-grid">
      <a href="#jeans" class="category-card">Jeans</a>
      <a href="#shirts" class="category-card">Shirts</a>
      <a href="#tshirts" class="category-card">T-Shirts</a>
      <a href="#shorts" class="category-card">Shorts</a>
      <a href="#shoes" class="category-card">Shoes</a>
      <a href="#watches" class="category-card">Watches</a>
    </div>
  </div>
</section>

<section id="offers" class="offers">
  <div class="container">
    <h2 class="section-title">Special Offers</h2>
    <div class="offers-grid">
      <div class="offer-card">
        <img src="https://i.pinimg.com/originals/94/3d/18/943d18d66e94b720702f14388e517f99.jpg" alt="50% OFF T-Shirts" class="offer-img">
        <h3>50% OFF T-Shirts</h3>
        <p>Limited time offer</p>
        <a href="#products" class="btn">Shop Now</a>
      </div>
      <div class="offer-card">
        <img src="" alt="Buy 1 Get 1 Free" class="offer-img">
        <h3>Buy 1 Get 1 Free</h3>
        <p>On selected items</p>
        <a href="#products" class="btn">Shop Now</a>
      </div>
      <div class="offer-card">
        <img src="" alt="Free Shipping" class="offer-img">
        <h3>Free Shipping</h3>
        <p>On orders above $100</p>
        <a href="#products" class="btn">Shop Now</a>
      </div>
    </div>
  </div>
</section>

<!-- Shirts Section -->
<section id="shirts" class="products">
  <div class="container">
    <h2 class="section-title">Shirts</h2>
    <div class="product-grid">
      <% products.filter(p => p.category === 'Shirts').forEach(product => { %>
        <div class="product-card">
          <a href="/product/<%= product._id %>" style="text-decoration: none; color: inherit;">
            <img src="<%= product.image %>" class="product-img" alt="<%= product.name %>">
            <h3 class="product-name"><%= product.name %></h3>
            <p class="product-price">$<%= product.price %></p>
          </a>
          <button class="add-to-cart-btn" data-name="<%= product.name.replace(/'/g, "\\'") %>" data-price="<%= product.price %>">
            Add to Cart
          </button>
        </div>
      <% }) %>
    </div>
  </div>
</section>

<!-- T-Shirts Section -->
<section id="tshirts" class="products">
  <div class="container">
    <h2 class="section-title">T-Shirts</h2>
    <div class="product-grid">
      <% products.filter(p => p.category === 'T-Shirts').forEach(product => { %>
        <div class="product-card">
          <a href="/product/<%= product._id %>" style="text-decoration: none; color: inherit;">
            <img src="<%= product.image %>" class="product-img" alt="<%= product.name %>">
            <h3 class="product-name"><%= product.name %></h3>
            <p class="product-price">$<%= product.price %></p>
          </a>
          <button class="add-to-cart-btn" data-name="<%= product.name.replace(/'/g, "\\'") %>" data-price="<%= product.price %>">
            Add to Cart
          </button>
        </div>
      <% }) %>
    </div>
  </div>
</section>

<!-- Jeans Section -->
<section id="jeans" class="products">
  <div class="container">
    <h2 class="section-title">Jeans</h2>
    <div class="product-grid">
      <% products.filter(p => p.category === 'Jeans').forEach(product => { %>
        <div class="product-card">
          <a href="/product/<%= product._id %>" style="text-decoration: none; color: inherit;">
            <img src="<%= product.image %>" class="product-img" alt="<%= product.name %>">
            <h3 class="product-name"><%= product.name %></h3>
            <p class="product-price">$<%= product.price %></p>
          </a>
          <button class="add-to-cart-btn" data-name="<%= product.name.replace(/'/g, "\\'") %>" data-price="<%= product.price %>">
            Add to Cart
          </button>
        </div>
      <% }) %>
    </div>
  </div>
</section>

<!-- Shorts Section -->
<section id="shorts" class="products">
  <div class="container">
    <h2 class="section-title">Shorts</h2>
    <div class="product-grid">
      <% products.filter(p => p.category === 'Shorts').forEach(product => { %>
        <div class="product-card">
          <a href="/product/<%= product._id %>" style="text-decoration: none; color: inherit;">
            <img src="<%= product.image %>" class="product-img" alt="<%= product.name %>">
            <h3 class="product-name"><%= product.name %></h3>
            <p class="product-price">$<%= product.price %></p>
          </a>
          <button class="add-to-cart-btn" data-name="<%= product.name.replace(/'/g, "\\'") %>" data-price="<%= product.price %>">
            Add to Cart
          </button>
        </div>
      <% }) %>
    </div>
  </div>
</section>

<!-- Shoes Section -->
<section id="shoes" class="products">
  <div class="container">
    <h2 class="section-title">Shoes</h2>
    <div class="product-grid">
      <% products.filter(p => p.category === 'Shoes').forEach(product => { %>
        <div class="product-card">
          <a href="/product/<%= product._id %>" style="text-decoration: none; color: inherit;">
            <img src="<%= product.image %>" class="product-img" alt="<%= product.name %>">
            <h3 class="product-name"><%= product.name %></h3>
            <p class="product-price">$<%= product.price %></p>
          </a>
          <button class="add-to-cart-btn" data-name="<%= product.name.replace(/'/g, "\\'") %>" data-price="<%= product.price %>">
            Add to Cart
          </button>
        </div>
      <% }) %>
    </div>
  </div>
</section>

<!-- Watches Section -->
<section id="watches" class="products">
  <div class="container">
    <h2 class="section-title">Watches</h2>
    <div class="product-grid">
      <% products.filter(p => p.category === 'Watches').forEach(product => { %>
        <div class="product-card">
          <a href="/product/<%= product._id %>" style="text-decoration: none; color: inherit;">
            <img src="<%= product.image %>" class="product-img" alt="<%= product.name %>">
            <h3 class="product-name"><%= product.name %></h3>
            <p class="product-price">$<%= product.price %></p>
          </a>
          <button class="add-to-cart-btn" data-name="<%= product.name.replace(/'/g, "\\'") %>" data-price="<%= product.price %>">
            Add to Cart
          </button>
        </div>
      <% }) %>
    </div>
  </div>
</section>

<footer class="footer">
  <div class="container footer-container">
    <div class="footer-section">
      <h3>StyleHub</h3>
      <p>Men's fashion store for modern style</p>
    </div>
    <div class="footer-section">
      <h4>Quick Links</h4>
      <a href="#home">Home</a>
      <a href="#categories">Categories</a>
      <a href="#offers">Offers</a>
      <a href="#products">Products</a>
    </div>
    <div class="footer-section">
      <h4>Contact</h4>
      <p>Email: a66s99a4@gmail.com</p>
      <p>Phone: +973 66937709</p>
    </div>
  </div>
  <p class="footer-bottom">© 2026 The brand is managed by Abdulaziz, Mohammed, and Abdulla</p>
</footer>



<script>
let cart = JSON.parse(localStorage.getItem("cart")) || [];

function addToCart(name, price) {
  cart.push({ name, price });
  localStorage.setItem("cart", JSON.stringify(cart));
  updateCartCount();
  alert(`${name} added to cart!`);
}

function updateCartCount() {
  const cartCountElement = document.getElementById("cart-count");
  if (cartCountElement) {
    cartCountElement.innerText = cart.length;
  }
}


document.querySelectorAll('.add-to-cart-btn').forEach(button => {
  button.addEventListener('click', function(e) {
    e.stopPropagation();
    const name = this.getAttribute('data-name');
    const price = parseInt(this.getAttribute('data-price'));
    addToCart(name, price);
  });
});

updateCartCount();
</script>
</body>
</html>
